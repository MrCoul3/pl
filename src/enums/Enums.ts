export enum EnumPlayerStates {
  stop = "stop",
  play = "play",
  pause = "pause",
}

export enum EnumHotKeys {
  space = "Space",
}

export enum EnumScaleConstants {
  minutes = 150000,
  hours   = 1800000,
  days    = 21600000,
}
